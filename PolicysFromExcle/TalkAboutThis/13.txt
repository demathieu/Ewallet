AliceSafe = new policyBuilder.policy(whitelist).allow('amount').install(alice);  // I need to make a hook that allows to differentiate between Read and Write access or Get and Set

AliceSafe = new policyBuilder.policy(whitelist).allow('deposit').install(alice);


